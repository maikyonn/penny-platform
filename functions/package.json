{
  "name": "penny-functions",
  "version": "1.0.0",
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "firebase emulators:exec --only functions 'npm run build'",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:stripe": "vitest run src/__tests__/stripe.test.ts",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0",
    "googleapis": "^144.0.0",
    "stripe": "^19.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.3"
  }
}
