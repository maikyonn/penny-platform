{
  "name": "penny-functions",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "firebase emulators:exec --only functions 'bun run build'",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:stripe": "vitest run src/__tests__/stripe.test.ts",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^19.1.0",
    "googleapis": "^144.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@firebase/rules-unit-testing": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.3"
  },
  "engines": {
    "node": "20"
  }
}

